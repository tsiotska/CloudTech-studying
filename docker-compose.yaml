version: "3"
services:
  nginx:
    build:
      context: ./
      dockerfile: ./deploy/nginx/Dockerfile
    ports:
      - 80:80
      - 443:443
    volumes:
      - C:/Users/Vitaliy/ssl/:/ssl
      # - /etc/nginx/ssl:/ssl
    restart: always
